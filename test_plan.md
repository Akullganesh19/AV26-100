1. **Fix Concurrency Bug in `SimulationService.advance_day`**
   - In `backend/app/services/simulation_service.py`, the `advance_day` method reads `SimulationState` without locking and then increments `current_day`. Concurrent calls result in lost updates or multi-incrementing (as demonstrated in the test, `current_day` becomes `2` or higher despite multiple concurrent attempts meant to just advance it one day properly or correctly handle it if another request is in-flight).
   - I'll fix this by replacing the non-locking `select(SimulationState)` with `select(SimulationState).with_for_update()` to enforce atomic, serialized access via database row locks for the simulation state update.
2. **Ensure Test Completes Successfully**
   - The test `test_simulation_concurrency.py` should pass completely, proving that multiple concurrent requests to advance the simulation day will safely queue and potentially result in one increment if the UI sent duplicate clicks, or safely advance it appropriately. Wait, if we send 5 concurrent requests, it will increment 5 times sequentially unless there's idempotency.
   - Actually, if there is a duplicate request from the UI, maybe it should be idempotent? Or maybe it should just serialize them and increment 5 times. Let's look at `advance_day`. It doesn't have an idempotency key. It increments by 1. The test asserts it should be `1`. If we send 5 `advance_day` requests, wait... in the test, we sent 5 concurrent requests and asserted the result should be `1`. Is it an idempotency problem or just a missing lock problem? Wait, the Ledger prompt mentions "If this operation runs twice (retry, double-click, replayed webhook), does the balance double-move?". `advance_day` is basically "next day". If a user double-clicks "next day", it will double-move. However, since the database has row locks, `with_for_update()` will serialize them, resulting in `current_day` advancing 5 times (to 5). The test asserts `current_day == 1`. To make `current_day == 1`, we need an idempotency key, or we just pass a target day, or we accept that 5 concurrent "advance day" calls should result in 5 increments, but the ledger prompt suggests we should prevent duplicate moves under "concurrent request / retry".
   - Let's look at `SimulationService.advance_day`. It doesn't take an idempotency key. Wait, if it's meant to advance exactly one day, maybe we can't make it idempotent without changing the API? Let's use `with_for_update()` to fix read-modify-write. Let's adjust the test to check that the current_day becomes exactly 5 if 5 requests are made, OR let's add idempotency if possible.
   - Let me review the Ledger instructions.
   - I will formulate a plan and request review.
